// V1 entrypoint for workspace routes. The implementation lives
// in the workspace module to keep route/controller/service
// co-located (same pattern as the auth module).
export { default } from "../../modules/workspace/workspace.routes.js";
